# ContactSection

Sektion „Kontakt“ (Anker `#kontakt`): Überschrift, links das ContactForm auf einem weißen Panel, rechts ContactInfo.

- **Props:** `form` (Props für ContactForm, z. B. `onSubmit`), `id`.
- Hintergrund `sky-soft`; Desktop 7/11 zu 4/11, der Info-Kasten bleibt beim Scrollen stehen. Mobil: Formular zuerst, darunter der Kasten.
